<Page
    x:Class="Greetings.Controls.PlaseDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Greetings.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image Source="{x:Bind Place.ImageSource, Mode=OneWay}" Stretch="Uniform"/>

        <StackPanel Grid.Column="1" Background="Beige" >
            <TextBlock Foreground="Black" FontSize="20" Text="{x:Bind Place.Name, Mode=OneWay}" />
            <TextBlock Foreground="Black" FontSize="20" Text="{x:Bind Place.Cost, Mode=OneWay}"/>
            <TextBlock Foreground="Black" FontSize="20" Text="{x:Bind Place.Address, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</Page>
